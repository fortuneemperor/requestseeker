import express from 'express';

import { name, version } from './package.json';

class App {

}

module.exports = App;

// const app = express();

// app.get('/version', async (req, res, next) => { // eslint-disable-line
//   return res.json({
//     name,
//     version,
//   });
// });

// module.exports = app;
